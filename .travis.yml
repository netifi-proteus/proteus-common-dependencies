language: java
sudo: required
jdk: oraclejdk8
install: "echo -e 'Starts Script Without Installation'"
git:
  depth: false
script: ci/travis.sh
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
notifications:
  email: false
cache:
  directories:
    - "$HOME/.gradle/caches/"
    - "$HOME/.gradle/wrapper/"
    - "$HOME/.m2"
